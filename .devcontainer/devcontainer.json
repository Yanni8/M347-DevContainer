{
	"name": "Existing Dockerfile",
	"dockerComposeFile": [
		"../DevEnvironnement/docker-compose.yml"
	],
	
	"workspaceFolder": "/app/development",

	"service": "app",

	"remoteUser": "application",

	//Because the Devcontainer environnement overrides the default Entrypoint...
	"onCreateCommand": "echo 'watcher &' >> ~/.bashrc"
}
